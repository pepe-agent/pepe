# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/
*.coverdata

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
pepe-*.tar

# Ignore the escript binary (built via "mix escript.build").
/pepe

# Ignore any stray local Mnesia dir/crash dump (the real store lives in ~/.pepe/data).
/Mnesia.*
MnesiaCore.*

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# phx.digest also writes hashed copies of static/ root files (favicon, robots)
# straight into priv/static/ - ignore those, keep the plain source files.
/priv/static/favicon-*.ico
/priv/static/robots-*.txt
/priv/static/robots-*.txt.gz
/priv/static/robots.txt.gz

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/


# Local-only agent instructions (not versioned)
CLAUDE.md

# Built escript binary (mix escript.build)
/pepe
/cortex

# Stray test-approval artifacts
arquivo_teste_aprovacao_*.txt

# Local Playwright MCP output (screenshots/snapshots from browser-driven checks)
/.playwright-mcp/

# Dialyzer's cached PLT (rebuilt locally/in CI, not versioned).
/priv/plts/

.playwright-mcp/

# Transcripts of voice notes dropped in the working tree while chatting.
/voice_*.json
/voice_*.srt
/voice_*.tsv
/voice_*.txt
/voice_*.vtt

# Local agent tooling state, not part of the project.
/.claude/agent-memory/
